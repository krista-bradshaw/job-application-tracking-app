# JWT signing secret - REQUIRED, server will refuse to start without this
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Port for the Express server (default: 3000)
PORT=3000
